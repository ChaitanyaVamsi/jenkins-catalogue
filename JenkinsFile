@Library('jenkins-shared-lib') _
def config = [
    project:"roboshop",
    component: "catalogue"
]

if( !env.BRANCH_NAME.equalsIgnoreCase('main') ){
  nodeJsEksPipeline(config)
}
else{
  echo "Follow proper process"
}